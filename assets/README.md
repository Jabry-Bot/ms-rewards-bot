# assets/

- **`logo.png`** — logo de la app (fuente). Colócalo aquí.
- **`icon.ico`** — icono generado a partir de `logo.png` por `scripts/build.py` (no se edita a mano). Es el icono de los `.exe` y de las ventanas.

Para regenerar el icono y los ejecutables: `python scripts/build.py`.
